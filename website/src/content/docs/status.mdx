---
title: Status & changelog
description: Where Causa stands and where it's going.
---

## Pre-0.1

Expect breaking changes without notice. The **0.1 gate is functional
completeness: multimodal I/O and subagents.**

During the `0.x` series, breaking changes to the wire serde shapes
(`ContextEvent` / `TurnResult` / `TurnTrace` family) bump the **minor**
version and are flagged at the top of the release notes — those shapes are a
load-bearing external contract pinned by serialization tests.

## Changelog

The canonical changelog lives in the repo:
[CHANGELOG.md](https://github.com/Preference-cmd/causa/blob/main/CHANGELOG.md)
(Keep a Changelog format).

## Contributing

See
[AGENTS.md](https://github.com/Preference-cmd/causa/blob/main/AGENTS.md) for
layout, commands (`cargo test --workspace`, clippy, fmt, the layering guard),
and workflow: small work commits straight to `main` via PR; non-trivial
behavior or contract changes open an issue first with observable acceptance
criteria. Conventional Commits, one logical change per commit.
